services:
  backend:
    build: ./backend
    container_name: deepfake_api
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: deepfake_ui
    ports:
      - "8501:8501"
    depends_on:
      - backend